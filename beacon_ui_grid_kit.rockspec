package = "beacon-ui-grid-kit"
version = "0.1-1"
source = { url = "." }
description = { summary = "Develop a Lua command-oriented project for grid scenarios with seeded input scenarios, deterministic summary checks, and fixture-scale datasets.", license = "MIT" }
build = { type = "builtin", modules = { policy = "src/policy.lua" } }
